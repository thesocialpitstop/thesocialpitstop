import { Card } from "@mui/material";
import styled from "styled-components";

export const EventItemAdminCard = styled(Card)`
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    padding: 16px;
    align-items: center;
`;

export const EventItemAdminDetail = styled.div``;